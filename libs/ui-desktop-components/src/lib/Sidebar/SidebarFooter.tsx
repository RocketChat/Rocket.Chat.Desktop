import { FC } from 'react';
import { ButtonGroup, Icon } from "@rocket.chat/fuselage";

// align?: 'start' | 'center' | 'end';
// stretch?: boolean;
// wrap?: boolean;
// vertical?: boolean;
// small?: boolean;
// medium?: boolean;

export const SidebarFooter: FC = (props) => <ButtonGroup vertical align='center' {...props}/>
export const SidebarGroup: FC = (props) => <ButtonGroup vertical align='center' {...props}/>
